
/**
 * # Core Philosophy
 * This ruleset enforces a strict user-ownership model for all personal data,
 * ensuring that users can only access their own information. Public content,
 * such as a central library of workouts and meals, is segregated into separate
 * top-level collections. This content is readable by any authenticated user,
 * but write access is disabled for all clients, pending a secure admin implementation.
 *
 * # Data Structure
 * - /users/{userId}: Stores private user profiles and serves as the root for all user-owned subcollections.
 *   - /users/{userId}/connections/{friendId}: Manages user friendships.
 *   - /users/{userId}/workoutProgress/{workoutProgressId}: Contains a user's private workout history.
 *   - /users/{userId}/orders/{orderId}: Contains a user's private order history.
 * - /chats/{chatId}/messages/{messageId}: Stores private messages between two users.
 * - /workouts/{workoutId}: A read-only collection of available workouts, writable only by backend processes (e.g., an admin SDK).
 * - /meals/{mealId}: A read-only collection of available meals, writable only by backend processes (e.g., an admin SDK).
 * - /mealPlans/{mealPlanId}: Meal plan assignments for users. Writable by admins/coaches.
 * - /locations/{locationId}: A collection of user-submitted courts. Readable by all, writable by authenticated users for their own submissions.
 * - /ballIsLife/{postId}: A collection for real-time user posts. Readable by all, writable only by the post creator.
 * - /products/{productId}: A read-only collection of merchandise, writable only by an admin.
 *
 * # Key Security Decisions
 * - **Admin Override**: Admins have full read/write access to all data paths.
 * - **User Data Isolation**: All data under `/users/{userId}` is strictly controlled by path-based security for non-admins.
 * - **Chat Security**: Chats are only accessible to the two participants involved.
 * - **Connection Security**: Users can manage their own connection documents (send, accept, delete requests).
 * - **Default Deny**: Any operation not explicitly granted is denied.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions for readable and reusable logic
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function getRole() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }

    function isAdmin() {
      // Check for custom claim OR role document for flexibility.
      // Custom claims are more efficient for rules.
      return isSignedIn() && (request.auth.token.admin == true || getRole() == 'admin');
    }
    
    function isCoach() {
      return isSignedIn() && (getRole() == 'coach' || isAdmin());
    }
    
    function isSeller() {
      return isSignedIn() && (getRole() == 'seller' || isAdmin());
    }

    function isParticipant(chatId) {
      return isSignedIn() && request.auth.uid in string(chatId).split('_');
    }
    
    // Admin has full access to everything. This rule is intentionally broad for admin users.
    match /{path=**} {
      allow read, write: if isAdmin();
    }
    
    match /{path=**}/connections/{connectionId} {
        allow list: if isCoach();
    }

    match /users/{userId} {
      allow get: if isSignedIn(); // Allow reading any user's profile for friend lists or by sellers
      allow list: if isSignedIn(); // Allow listing users to find friends
      allow create: if isOwner(userId); // Allow a user to create their own document
      allow update: if isOwner(userId) || isCoach(); // Coaches can also update user profiles
      allow delete: if isAdmin(); // Only admins can delete user documents

      match /connections/{friendId} {
        allow read, delete: if isOwner(userId);
        // Allow creating/updating a connection if you are the user or the friend
        allow write: if isOwner(userId) || isOwner(friendId);
      }
      
      match /workoutProgress/{workoutProgressId} {
        allow read, write: if isOwner(userId);
      }
      
      // Allow sellers/admins to list any user's orders via collection group query
      match /orders/{orderId} {
        allow read, list: if isOwner(userId) || isSeller();
        allow create: if isOwner(userId);
        allow update: if isSeller(); // Sellers and Admins can update status
        allow delete: if false; // Preserve order history
      }
    }

    match /chats/{chatId}/messages/{messageId} {
      allow read, write: if isParticipant(chatId);
    }
    
    match /mealPlans/{mealPlanId} {
      allow get: if isSignedIn();
      allow list: if isCoach(); // Admins and coaches can list all plans
      // Only the authoring coach/admin can modify
      allow create, update, delete: if isCoach() && request.resource.data.authorId == request.auth.uid; 
    }

    match /workouts/{workoutId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isCoach(); // Managed by Coach or Admin
    }

    match /meals/{mealId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isCoach(); // Managed by Coach or Admin
    }

    match /locations/{locationId} {
      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      // An admin can update any location, or a user can update their own submission.
      allow update: if (isSignedIn() && resource.data.creatorId == request.auth.uid) || isAdmin();
      // An admin can delete any location, or a user can delete their own submission.
      allow delete: if (isSignedIn() && resource.data.creatorId == request.auth.uid) || isAdmin();
    }
    
    match /ballIsLife/{postId} {
      allow get, list: if isSignedIn();
      allow create: if isSignedIn() && request.resource.data.creatorId == request.auth.uid;
      allow update, delete: if isSignedIn() && resource.data.creatorId == request.auth.uid;
    }

    match /products/{productId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isSeller(); // Managed by Seller or Admin
    }
  }
}
