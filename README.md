# Baseline

This is a Next.js application for Baseline, a personal basketball training app.

To get started, configure your Firebase credentials in a `.env.local` file at the root of the project. Then, run the development server.
